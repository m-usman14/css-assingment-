<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create Assignment</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #2e8b57, #1e90ff);
      font-family: sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .modal {
      background: white;
      padding: 30px;
      width: 350px;
      border-radius: 10px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
      position: relative;
    }

    .modal h2 {
      margin: 0 0 20px;
      font-size: 18px;
      text-align: center;
    }

    .close {
      position: absolute;
      top: 15px;
      right: 15px;
      font-size: 18px;
      cursor: pointer;
    }

    label {
      display: block;
      font-size: 14px;
      margin-bottom: 5px;
      color: #555;
    }

    select {
      width: 100%;
      padding: 8px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .assignee-mode {
      display: flex;
      gap: 10px;
      margin-bottom: 20px;
    }

    .assignee-mode div {
      flex: 1;
      padding: 10px;
      border: 1px solid #ccc;
      text-align: center;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
    }

    .selected {
      border: 2px solid #1e90ff;
      background-color: #e6f2ff;
      font-weight: bold;
    }

    button {
      width: 100%;
      padding: 12px;
      background: #1e90ff;
      border: none;
      color: white;
      border-radius: 5px;
      font-size: 16px;
      cursor: pointer;
    }
  </style>
</head>
<body>

<div class="modal">
  <span class="close">&times;</span>
  <h2>Create Assignment</h2>

  <label for="class">Class</label>
  <select id="class">
    <option>US History</option>
  </select>

  <label>Assignee Mode</label>
  <div class="assignee-mode">
    <div class="selected">All Students<br><small>Every enrolled student</small></div>
    <div>Individual Students<br><small>Specific students</small></div>
  </div>

  <button>Continue</button>
</div>

</body>
</html>
